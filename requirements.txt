# Ranges are pinned to major versions known to work with this code.
# To lock exact versions for reproducibility, activate your venv and run:
#   pip freeze > requirements.txt

openai>=1.60,<3
faiss-cpu>=1.9,<2
numpy>=1.26,<3
tiktoken>=0.7,<1

# Document parsing
pdfplumber>=0.11,<1
python-docx>=1.1,<2
pillow>=10.0,<12

# Scraping
requests>=2.32,<3
beautifulsoup4>=4.12,<5

# App
streamlit>=1.40,<2
python-dotenv>=1.0,<2
